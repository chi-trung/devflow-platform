namespace DevFlow.Domain.Enums;

public enum TaskItemStatus
{
    Idea = 0,
    Planning = 1,
    Approval = 2,
    Ready = 3,
    InProgress = 4,
    Review = 5,
    Done = 6
}

public enum TaskItemPriority
{
    Low = 0,
    Medium = 1,
    High = 2,
    Critical = 3
}
