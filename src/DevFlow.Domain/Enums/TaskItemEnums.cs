namespace DevFlow.Domain.Enums;

public enum TaskItemStatus
{
    Backlog = 0,
    InProgress = 1,
    InReview = 2,
    Done = 3
}

public enum TaskItemPriority
{
    Low = 0,
    Medium = 1,
    High = 2,
    Critical = 3
}
